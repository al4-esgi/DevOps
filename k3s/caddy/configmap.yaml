apiVersion: v1
kind: ConfigMap
metadata:
  name: caddy-config
  namespace: infra
data:
  Caddyfile: |
    *.devops.futurpanda.dev {
      tls {
        dns vercel
    }

    @n8n host n8n.devops.futurpanda.dev 
    handle @n8n {
      reverse_proxy n8n.devops.svc.cluster.local:5678
    }
